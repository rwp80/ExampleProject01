extends Node3D

# TODO maybe automate spawning of enemies later

#const enemy_resource = preload("res://enemy.tscn")
#var enemy: Array[CharacterBody3D]

#func spawn_enemy(spawn_position: Vector3):
#	enemy.append(enemy_resource.new())
#	enemy[enemy.size()].global_position = spawn_position
#	add_child(enemy[enemy.size()])
